rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /donors/{donorId} { allow read, write: if true; }
    match /blood_requests/{requestId} { allow read, write: if true; }
  }
}
